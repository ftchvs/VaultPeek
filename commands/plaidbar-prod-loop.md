---
description: Deprecated alias. The production loop moved to commands/vaultpeek-prod-loop.md after the PlaidBar to VaultPeek rename.
---

# /plaidbar-prod-loop (deprecated)

The product was renamed PlaidBar to VaultPeek. This command moved to
[`commands/vaultpeek-prod-loop.md`](vaultpeek-prod-loop.md).

If you are an agent reading this file as a prompt, read
`commands/vaultpeek-prod-loop.md` and follow it instead. This pointer exists so
older scheduled runs and prompts that reference the old path keep working; do
not add new references to this file.
