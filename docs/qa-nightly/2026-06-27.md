# Nightly Capability QA — 2026-06-27

Automated nightly QA pass on VaultPeek (formerly PlaidBar). One realistic
scenario per major capability, run under identical conditions, root-causing every
failure until the quality bar is met.

- **Branch:** `qa/nightly-capability-2026-06-27` (off `origin/main` @ `9791d523`)
- **Toolchain / conditions:** `swift build -Xswiftc -strict-concurrency=complete -Xswiftc -warnings-as-errors --disable-keychain` then `swift test --skip-update --disable-keychain`; demo via `.build/debug/PlaidBar --demo --render-window-first <dir>` (+ legacy `--render-snapshot`); server via a credential-less `PlaidBarServer --sandbox` boot on an isolated data dir. Synthetic/demo data only — no real Plaid credentials, tokens, account IDs, or balances.
- **App version under test:** 1.0.0

## Quality bar (defined before running)

> 100% of capability scenarios pass with zero strict-concurrency build warnings.

**Evaluation method (one consistent method for the whole set):** deterministic
pass/fail assertions. Each unit-level capability scenario passes only if its
representative Swift Testing suite passes inside a single identical-conditions
full `swift test` run (231 suites / 2653 tests). The two process-level scenarios
(C9 server surface, C13 demo launch) pass only if every explicit assertion in
their runner holds (HTTP codes, file permissions, allowed-key set, rendered
output).

## Result summary

| Gate | Result |
|------|--------|
| Strict-concurrency build (`-warnings-as-errors`) | **PASS** — `Build complete! (21.14s)`, exit 0, **zero compiler warnings** |
| Full test suite (`swift test`) | **PASS** — 2653 tests, 231 suites, **0 failures**, exit 0 |
| Capability scenarios | **13 / 13 PASS** |

> **Skips are expected, not failures.** Under `--disable-keychain`, 12 tests that
> exercise live macOS Keychain writes are intentionally skipped with the reason
> `"macOS Keychain accepts test writes"`. The run summary is unambiguous —
> *"Test run with 2653 tests passed"* — and no test recorded an issue.

> **Note on the lone manifest line:** SwiftPM emits one cosmetic note —
> `dependency 'async-http-client' is not used by any target`. This is **not** a
> strict-concurrency compiler warning and is **intentional**: `async-http-client`
> is pinned `exact: "1.33.1"` in `Package.swift` to hold the transitive
> resolution on the Swift 6.0 baseline. The pin is load-bearing. Compiler-warning
> count under the strict gate is zero.

## Capabilities → scenarios → evidence

N = 13 major capabilities derived from the current code (`PlaidBarCore`
utilities/models, the `PlaidBarCache` target, the `PlaidBarServer` surface, and
the demo app process). The capability surface is unchanged vs. the prior pass.

| # | Capability | Scenario (expected outcome) | Representative suite / runner | Result |
|---|------------|-----------------------------|-------------------------------|--------|
| C1 | Account & balance summaries | Demo accounts reduce to correct net worth + balance mix; menu-bar summary text is derived, not raw | `Wealth summary presentation`, `MenuBarAnnouncement Tests` | PASS |
| C2 | Transaction sync + cursor reduction | `added`+`modified` upsert by id, `removed` drop, pending→posted dedupe yield a correct ordered set; staleness derived from last-sync | `Sync response payloads`, `Sync History Diff Tests`, `SyncStaleness Tests` | PASS |
| C3 | Recurring-charge detection | Periodic merchants roll up to recurring commitments; insufficient history yields the empty state | `Recurring detector helpers`, `Recurring commitment rollup` | PASS |
| C4 | Category/budget rollups & spend math | Override-aware (persisted-only) spend math; exclude/transfer decisions move SPENT/LEFT; suggested vs explicit budgets resolve correctly | `Override-aware spend math`, `Effective Category Resolver Tests`, `Category budget presentation` | PASS |
| C5 | AI / local insights | Local insight receipt + Foundation Models structured insight + NL/FM merchant categorization stay local-only and deterministic | `Foundation Models Structured Insight Tests`, `NL Merchant Categorizer Tests`, `FM Merchant Categorizer Tests` | PASS |
| C6 | Number / currency / date formatters | Currency, percent, relative-date, and transaction-date parsing format deterministically | `Formatters` | PASS |
| C7 | Presentation & state mapping | Typed `Route` navigation + deep-link contract + status-readiness/connection presentation map correctly | `Route`, `Route destinations`, `StatusPanelText Tests`, `Dashboard status readiness` | PASS |
| C8 | Attention-queue state surfacing | Recovery/sync/financial attention rows surface in deterministic priority order, threshold edges inclusive, spend window bounded; copy stays amount-free | `AttentionQueue Tests` | PASS |
| C9 | Server surface (live) | `/health` open (200); `/api/status` fails closed without/with-wrong token (401) and returns metadata-only with a valid token; Plaid routes 503 naming missing creds; `auth-token` 0600, data dir 0700 | credential-less `PlaidBarServer --sandbox` runner | PASS |
| C10 | Privacy & security (mask/lock) | Strong-mask formatter + privacy-mask presentation + App-Lock/Privacy-Mask window parity hide values without leaking figures | `Strong mask formatter`, `Privacy Mask Presentation Tests`, `Window-first App Lock / Privacy Mask parity` | PASS |
| C11 | Goals (local-first) | Goal progress math, editor input validation, summary rollup, and local-first persistence behave deterministically | `Goal progress math`, `Goals local-first persistence`, `Goals summary rollup` | PASS |
| C12 | Read-model cache (`PlaidBarCache`) | Disposable read-model + per-transaction cache store upsert/dedup and survive the clear-wins cold-start race | `Disposable ... read-model cache store`, `Disposable per-transaction cache store`, `Read-model cache clear-wins race` | PASS |
| C13 | Demo fixtures / app boot (live) | `--demo` boots with no Plaid/server/credentials and renders populated window-first surfaces | `--demo --render-window-first` + `Demo Review / Budget Fixtures` | PASS |

### C9 server-surface evidence (credential-less `--sandbox`, port 18585, isolated data dir)

```
S1 /health (unauth)                         -> HTTP 200
S2 /api/status (no token)                   -> HTTP 401   (fails closed)
S3 /api/status (wrong bearer)               -> HTTP 401   (rejected)
S4 /api/status (valid token)                -> HTTP 200, metadata only:
   keys = [credentialsConfigured, environment, itemCount, storagePath,
           syncReady, syncedItemCount, version]   credentialsConfigured=false
S5 forbidden-field scan of status JSON      -> FORBIDDEN KEYS: NONE
S6 /api/accounts (valid token, no creds)    -> HTTP 503, body names PLAID_CLIENT_ID + PLAID_SECRET
S7 auth-token perms -rw-------  data dir drwx------   (0600 / 0700)
```

No tokens, account IDs, balances, or transactions are exposed by `/api/status`.
The Plaid route fails closed with a credential-setup message, not data.

### C13 demo-launch evidence

```
.build/debug/PlaidBar --demo --render-window-first <dir>   -> exit 0
  wrote window-dashboard.png      (1960x1600)   + review / transactions / budgets /
  planning / goals / insights / alerts / accounts (1960x1600 each)
  wrote window-shell.png          (2560x1600)
.build/debug/PlaidBar --demo --render-snapshot <dir>       -> exit 0
  wrote render-settings-appearance.png (1120x1280)
```

Visual confirmation (window-dashboard.png): fully populated fixtures — Net Worth
**$60,104** across 5 accounts, Safe to Spend **$24,415**, Last-30-day spend
**$4,799** across 36 transactions, the 365-day activity heatmap (153 active
days), Recurring obligations (Cloudvar, Whole Foods, Target, Uber, Con Edison),
all five Accounts with balances, the Goals starter card, and the
Spending-by-Category donut (**$4,122.21**). i.e. a populated workspace, not an
empty state. Net worth reconciles to assets − debt.

> The legacy `--render-snapshot` popover capture is now a no-op for the
> dashboard/flyout popovers ("no visible popover window to capture") because
> **window-first is the shipping default** (`WindowFirstFeatureFlag.defaultValue
> == true`), so the menu-bar surface is reduced to the glance and no popover
> window is shown headlessly. This is expected behavior, not a product
> regression: the populated-surface capability is fully exercised by the
> window-first renderer above. The settings render (which does not depend on the
> popover) still writes successfully.

## Root-cause fixes applied

**None.** Every scenario met its success criteria on the first identical-conditions
run. In particular, the C8 `AttentionQueue` wall-clock time-bomb diagnosed and
fixed on the 2026-06-20 pass is now present on `main` (the `now:` pins at
`AttentionQueueTests.swift:248/343/366`), so the two previously date-sensitive
tests pass deterministically without further change.

## Final state

- **All green.** Strict-concurrency build: 0 compiler warnings. Full suite:
  2653 tests / 231 suites / 0 failures (12 intentional Keychain-gated skips under
  `--disable-keychain`). 13/13 capability scenarios pass.
- **No code changes required** this cycle — the quality bar was met as-is.
- Working tree clean except this report on the QA branch. No PR opened (no code
  fixes to summarize). Nothing pushed to or merged into `main`.
- No blockers remaining.
