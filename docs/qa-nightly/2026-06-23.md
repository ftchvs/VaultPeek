# Nightly Capability QA — 2026-06-23

Automated nightly QA pass on VaultPeek (formerly PlaidBar). One realistic scenario
per major capability, all run under identical CI-gate conditions, evaluated with
deterministic pass/fail assertions.

- **Branch:** `qa/nightly-2026-06-23` (cut from `origin/main`)
- **Base SHA:** `95486e7` — *docs(qa): refresh README screenshots + QA render after post-1.0 visual changes (#642)*
- **Toolchain:** Apple Swift 6.3.2 (swiftlang-6.3.2.1.108), target `arm64-apple-macosx26.0`
- **Data:** demo / synthetic / credential-less only. No real Plaid credentials, tokens, account IDs, or balances were used.

## Quality bar (defined before running)

> **100% of scenarios pass with zero strict-concurrency build warnings.**

A scenario passes only if its explicit expected outcome is met exactly. The
evaluation method is uniform across the whole set: **deterministic pass/fail
assertions** — the Swift Testing suites that own each capability's logic (run as
the CI gate `swift test`), plus, for the server surface, a live credential-less
server boot asserted with `curl` status codes and a JSON key contract check.

## Conditions (identical for every scenario)

Replicates the CI gate exactly:

```bash
# Cross-cutting gate (all scenarios share this build)
swift build -Xswiftc -strict-concurrency=complete -Xswiftc -warnings-as-errors
swift test
```

- Strict-concurrency build: **PASS** — `Build complete! (20.86s)`, exit 0, **zero warnings** (warnings-as-errors would have failed the build otherwise).
- Test run: **2179 tests passed, 12 skipped, 0 failed** (exit 0). The 12 skips are environment-gated server Keychain tests (`skipped: "macOS Keychain accepts test writes"`), not failures.

## Capabilities → scenarios → results

N = 8 major capabilities derived from the current `Sources/PlaidBarCore` +
`Sources/PlaidBarServer` surface (not from memory).

| # | Capability | Scenario (expected outcome) | Evidence | Result |
|---|-----------|------------------------------|----------|--------|
| 1 | Account / balance summaries | Account presentation, reconciliation, balance composition, sparkline, wealth-summary mapping produce correct grouped/typed values from fixtures | Suites: *Account list grouping (AND-623)*, *Account reconciliation against server item authority*, *Balance composition presentation*, *Balance trend display values*, *AccountDetailInsights*, *WealthSummaryPresentation* — all passed | **PASS** |
| 2 | Transaction sync + sync-cursor reduction | Cursored `added/modified/removed` batches reduce to a deterministic, deduped transaction set; staleness math is correct | Suites: *Sync response payloads*, *Sync History Diff*, *SyncStaleness*, `TransactionSyncReducer`/`TransactionSyncBatch` coverage; server *Transaction sync preserves partial success and deterministic aggregation while bounded* + *Concurrent syncs … coalesce onto a single Plaid pass* — all passed | **PASS** |
| 3 | Recurring-charge detection | Recurring detector identifies cadence/commitments; insufficient-history yields the documented empty state | Suites: *Recurring detector helpers*, *Recurring commitment rollup (AND-559)*, *Recurring Obligations Presentation*, *Recurring Payments Surface Presentation*, *Recurring stream flags* — all passed | **PASS** |
| 4 | Category / budget rollups + spend math | Override-aware spend math (category/transfer/exclude) drives donut, status tree, and SPENT/BUDGET/LEFT totals; persisted-only resolution skips excluded/transfer rows | Suites: *Override-aware spend math (AND-526)*, *Category dashboard builder (AND-536)*, *CategoryDashboardTableModel … (AND-539)*, *SpendDonutModel … (AND-537)*, *Category budgets (AND-402)*, *Category budget merge*, *EffectiveCategoryResolver*, *Spending Summary Override* — all passed | **PASS** |
| 5 | AI / local insights | Local insight receipt is built from local rows only; on-device merchant/income categorizers (FM + NL) and structured FM insight map correctly and stay local | Suites: *Local AI insights model accessors*, *LocalAIInsightReceipt*, *Foundation Models Structured Insight*, *FM Spending Category Mapper*, *FM Income Categorizer*, *NLMerchantCategorizer*, *LocalAIRuntimeResolution*, *LocalAITierResolver*, *LocalInsightModel{Prompt,Runtime}* — all passed | **PASS** |
| 6 | Number / currency / date formatters | Currency, percent, signed-amount, relative-date, and strong-mask formatters produce expected strings (incl. masked placeholders) | Suites: *Formatters*, *Strong mask formatter*, *Strong mask formatter coverage* — all passed | **PASS** |
| 7 | Presentation & state mapping (PlaidBarCore) | Menu-bar status/glance/review-badge presentation, dashboard read-model mapping, typed routing/deep-links, and per-window navigation independence map state correctly | Suites: *MenuBarGlanceModel contract*, *MenuBarReviewBadge*, *MenuBarAnnouncement*, *DashboardReadModelMapper*, *Route*, *Route destinations*, *Route deep-link contract (AND-586)*, *R-10 per-window navigation independence* — all passed | **PASS** |
| 8 | Server surface (`/health` open, `/api/status` auth-gated, metadata-only) | Live credential-less boot: `/health` answers unauthenticated; `/api/status` rejects missing/invalid bearer; valid bearer returns only readiness metadata (no tokens / account IDs / balances) | Server suites passed (Keychain-dependent cases skipped, env-gated) **plus** the live boot below | **PASS** |

### Capability 8 — live server evidence (credential-less boot, port 18585, temp data dir)

| Check | Expected | Observed |
|-------|----------|----------|
| `GET /health` (no auth) | 200 | **HTTP 200** |
| `GET /api/status` (no token) | 401 | **HTTP 401** |
| `GET /api/status` (bad token) | 401 | **HTTP 401** |
| `GET /api/status` (valid token) | 200 + metadata body | **HTTP 200** |
| Status body keys | only readiness metadata | `credentialsConfigured, environment, itemCount, storagePath, syncReady, syncedItemCount, version` |
| Forbidden-field scan (token/secret/access/balance/account/transaction/client/institution/itemId/public) | none present | **NONE** |
| `credentialsConfigured` (no creds) | `false` | **false** |

## Root-cause fixes applied

**None required.** Every scenario met its expected outcome on the first run under
the defined conditions; no test was modified to mask a failure.

## Final state

**All green.** 8/8 capability scenarios pass; strict-concurrency build is clean
(zero warnings); `swift test` = 2179 passed / 12 env-skipped / 0 failed; the live
server contract holds (open `/health`, auth-gated metadata-only `/api/status`).
The quality bar is met. No code changes were needed, so **no pull request is
opened** per the run's reporting rule.

### Non-blocking observations (not gate failures, not in scenario scope)

- **Test-target deprecation warnings.** Compiling the *test* target surfaces
  `'defaultIconName' is deprecated: renamed to 'canonicalIconName'`
  (e.g. `Tests/PlaidBarCoreTests/DashboardStatusReadinessTests.swift:204`). These
  are warnings in test code only; the test target is **not** built with
  `-warnings-as-errors`, so they do not fail the strict-concurrency gate (which
  covers the product targets and was clean). Candidate for a small future
  hygiene cleanup; intentionally left untouched here to keep this QA run a
  no-code-change pass.
