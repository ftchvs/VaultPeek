# Nightly Capability QA — 2026-06-24

Automated nightly QA pass on VaultPeek (formerly PlaidBar). One realistic test
scenario per major capability, run under identical conditions, with a single
deterministic evaluation method.

- **Branch:** `qa/nightly-2026-06-24` (off `origin/main`)
- **Base commit:** `004f612` (`fix: make broad strict Swift gates pass`)
- **Toolchain:** Apple Swift 6.3.2 (swiftlang-6.3.2.1.108), target `arm64-apple-macosx26.0`
- **Data:** demo/synthetic fixtures only (`DemoFixtures`, in-memory SQLite). No real
  Plaid credentials, access tokens, account IDs, or balances.

## Quality bar (defined up front)

> **100% of capability scenarios pass with zero strict-concurrency build warnings.**

Evaluation method (one, consistent across the whole set): deterministic pass/fail
via the existing Swift Testing assertion suites. A scenario passes only if its
mapped suite(s) report zero failures. The global gate replicates CI exactly:

```bash
swift build -Xswiftc -strict-concurrency=complete -Xswiftc -warnings-as-errors --disable-keychain
swift test --skip-update --disable-keychain
```

## Identical conditions (run once for the whole set)

| Gate | Command | Result |
|------|---------|--------|
| Strict concurrency build | `swift build -Xswiftc -strict-concurrency=complete -Xswiftc -warnings-as-errors --disable-keychain` | **Build complete (23.59s), 0 warnings / 0 errors** |
| Full test suite | `swift test --skip-update --disable-keychain` | **Test run with 2180 tests passed**, 0 failed |

## Capabilities & scenarios

Capabilities were derived from the current `Sources/PlaidBarCore/` tree (not from
memory). N = 8 major capabilities. Each scenario was additionally re-run in
isolation via `--filter` for crisp evidence (build cached from the gate above).

| # | Capability | Scenario (expected outcome) | Source under test | Filtered command | Result |
|---|------------|-----------------------------|-------------------|------------------|--------|
| C1 | Account / balance summaries | Account rows, wealth-summary rail, and balance composition aggregate fixture balances into the documented net-worth / mix presentation | `WealthSummaryPresentation`, `AccountBalanceLedger`, `BalanceCompositionPresentation` | `--filter 'WealthSummaryPresentationTests\|AccountBalanceLedgerTests\|BalanceCompositionPresentationTests'` | ✅ 16 tests passed |
| C2 | Transaction sync + sync-cursor reduction | A multi-page sync response with added/modified/removed deltas reduces to a deterministic, deduplicated transaction set and a forward-only cursor | `SyncResponse`, `SyncHistoryDiff`, `TransactionSyncReducer`, `TransactionDerivedIndex` | `--filter 'SyncResponseTests\|SyncHistoryDiffTests\|TransactionDerivedIndexTests'` | ✅ 17 tests passed |
| C3 | Recurring-charge detection | Synthetic periodic charges are clustered into recurring obligations; insufficient history yields the documented empty/uncertain state | `RecurringDetector`, `RecurringCommitment` | `--filter 'RecurringDetectorHelpersTests\|RecurringCommitmentTests'` | ✅ 16 tests passed |
| C4 | Category / budget rollups & spend math | Override-aware spend (category/transfer/exclude decisions) rolls up to SPENT/LEFT against persisted budgets; excluded/transfer rows drop out of totals | `CategoryBudgetPlanner`, `EffectiveCategoryResolver`, override-aware spend math | `--filter 'CategoryBudgetPlannerTests\|EffectiveCategoryResolverTests\|OverrideAwareSpendMathTests'` | ✅ 66 tests passed |
| C5 | AI / local insights | Insight receipt is built locally from fixture transactions (source rows, window, top category, recurring estimate, local-only badge); no-runtime path degrades to documented disabled copy | `LocalAIInsightBuilder`, `LocalInsightModelRuntime` | `--filter 'LocalAIInsightReceiptTests\|LocalAIInsightsModelTests\|LocalInsightModelRuntimeTests'` | ✅ 34 tests passed |
| C6 | Number / currency / date formatters | Currency, signed amounts, percentages, and relative dates format to the documented strings (and stay stable/tabular) | `Formatters` | `--filter 'FormattersTests'` | ✅ 8 tests passed |
| C7 | Presentation & state mapping | Read-model mapper maps DTOs → dashboard read model; typed `Route` navigation transitions resolve deterministically | `DashboardReadModelMapper`, `NavigationState`, `Route` | `--filter 'DashboardReadModelMapperTests\|RouteNavigationTests'` | ✅ 52 tests passed |
| C8 | Server surface | `/health` is unauthenticated; `/api/*` requires the bearer token; `/api/status` encodes only release-approved readiness keys (never tokens, account IDs, balances, or transactions); access-token bytes never leak | `PlaidBarServer` routes, `APITokenMiddleware`, `ServerStatus`, `TokenStore` | `--filter 'PlaidBarServerTests\|TokenStorageSafetyTests'` | ✅ 168 tests passed |

## Root-cause fixes

None. All capabilities met their success criteria on the first run under the
identical CI conditions. No test was modified to mask a failure; no source change
was required.

## Final state

**All green.** 8/8 capability scenarios pass; full suite 2180/2180; strict-concurrency
build clean (0 warnings, warnings-as-errors). Quality bar met with no code changes.

Per the task contract, because everything passed with no code changes, **no pull
request is opened**. This report is committed to the QA branch only; nothing is
pushed to or merged into `main`.

### Notes / honest limits

- GUI- and system-dependent rows in `docs/qa-matrix.md` (Liquid Glass over live
  wallpaper, Reduce Transparency, Spotlight/Siri App Intents, Control Center,
  widget timelines, on-hardware three-column no-jump, VoiceOver) are out of scope
  for a headless agent and remain "needs human eyes" as documented there. This
  pass covers the deterministic, headless-testable capability surface.
- The demo data path is exercised deterministically by `DemoFixtures*` suites and
  by building the `PlaidBar` app target under strict concurrency; the menu-bar GUI
  itself was not launched (it requires a GUI session / Screen Recording).
